{
    "mcpServers": {
        "ahma_mcp": {
            "type": "stdio",
            "cwd": "${workspaceFolder}",
            "command": "target/release/ahma_mcp",
            "args": [
                "--synchronous"
            ],
            "dev": {
                "watch": [
                    "target/release/ahma_mcp"
                ],
                "command": "cargo",
                "args": [
                    "run",
                    "--release",
                    "--",
                    "--synchronous"
                ]
            }
        }
    },
    "inputs": []
}