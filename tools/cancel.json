{
    "name": "cancel",
    "command": "cancel",
    "description": "Cancel a running operation by its operation ID.",
    "synchronous": true,
    "guidance_key": "operation_management",
    "subcommand": [
        {
            "name": "default",
            "description": "Cancel a running operation by its operation ID.",
            "synchronous": true,
            "options": [
                {
                    "name": "operation_id",
                    "type": "string",
                    "description": "The ID of the operation to cancel",
                    "required": true
                },
                {
                    "name": "reason",
                    "type": "string",
                    "description": "Optional reason for cancellation (for debugging and LLM feedback)",
                    "required": false
                }
            ]
        }
    ]
}