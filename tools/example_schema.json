{
    "name": "example_tool",
    "description": "Example tool demonstrating comprehensive JSON schema usage",
    "command": "example",
    "enabled": true,
    "timeout_seconds": 600,
    "hints": {
        "default": "While example operations run, consider reviewing configuration files, planning next steps, or analyzing output patterns.",
        "build": "Building in progress - review build configuration, plan deployment steps, or work on documentation.",
        "test": "Tests running - analyze test patterns, consider additional test cases, or review code coverage strategies."
    },
    "subcommand": [
        {
            "name": "build",
            "description": "Build the project.\n\n**IMPORTANT:** This tool operates asynchronously.\n1. **Immediate Response:** Returns operation_id and status 'started'. NOT success.\n2. **Final Result:** Result pushed automatically via MCP notification when complete.\n\n**Your Instructions:**\n- DO NOT wait for the final result.\n- **DO** continue with other tasks that don't depend on this operation.\n- You **MUST** process the future result notification to know if operation succeeded.",
            "options": [
                {
                    "name": "release",
                    "type": "boolean",
                    "description": "Build in release mode with optimizations",
                    "default": false
                },
                {
                    "name": "jobs",
                    "type": "integer",
                    "description": "Number of parallel jobs (1-64)",
                    "minimum": 1,
                    "maximum": 64
                },
                {
                    "name": "features",
                    "type": "array",
                    "items": {
                        "type": "string",
                        "pattern": "^[a-zA-Z0-9_-]+$"
                    },
                    "description": "List of features to enable",
                    "minItems": 0,
                    "maxItems": 20,
                    "uniqueItems": true
                },
                {
                    "name": "target",
                    "type": "string",
                    "description": "Target platform for cross-compilation",
                    "enum": [
                        "x86_64-unknown-linux-gnu",
                        "aarch64-apple-darwin",
                        "x86_64-pc-windows-msvc"
                    ],
                    "default": "x86_64-unknown-linux-gnu"
                }
            ]
        },
        {
            "name": "test",
            "description": "Run tests.\n\n**IMPORTANT:** This tool operates asynchronously.\n1. **Immediate Response:** Returns operation_id and status 'started'. NOT success.\n2. **Final Result:** Result pushed automatically via MCP notification when complete.\n\n**Your Instructions:**\n- DO NOT wait for the final result.\n- **DO** continue with other tasks that don't depend on this operation.\n- You **MUST** process the future result notification to know if operation succeeded.",
            "options": [
                {
                    "name": "release",
                    "type": "boolean",
                    "description": "Test release build",
                    "default": false
                },
                {
                    "name": "package",
                    "type": "string",
                    "description": "Test specific package",
                    "pattern": "^[a-zA-Z0-9_-]+$"
                },
                {
                    "name": "lib",
                    "type": "boolean",
                    "description": "Test only library code",
                    "default": false
                },
                {
                    "name": "doc",
                    "type": "boolean",
                    "description": "Test documentation examples",
                    "default": false
                }
            ]
        },
        {
            "name": "version",
            "description": "Show tool version information - returns immediately.",
            "synchronous": true,
            "options": []
        },
        {
            "name": "status",
            "description": "Check current status - returns immediately.",
            "synchronous": true,
            "options": [
                {
                    "name": "verbose",
                    "type": "boolean",
                    "description": "Show detailed status information",
                    "default": false
                }
            ]
        }
    ]
}