{
	"servers": {
		"Ahma-release": {
			"type": "stdio",
			"command": "${workspaceFolder}/target/release/ahma_mcp",
			"args": [
				"--log-to-stderr",
			],
			"env": {
				"RUST_LOG": "debug"
			}
		}
	}
}